const React = require('react');

const Backing = React.createClass({
    render: function(){
        return(
          <section className="backing">
            <div className=""> Backing Section </div>
          </section>
      );
    }
});

module.exports = Backing;
